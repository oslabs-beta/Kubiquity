import React from 'react';

// Pod might have:
// pod ID
// pod name
// memory usage
// the pod's associated node

class Pod extends React.Component {
  render() {
    return (
      <div>Pod</div>
    )
  }
}

export default Pod;